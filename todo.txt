$ git branch -m master main

+ remove image.Point from stroke
+ draw lerping card last so it appears on top
+ moving card from pile to pile
+ dragging >1 card
+ CardState resting, lerping, flipping in, flipping out, shake left, shake right, shake center
  ... but can flip and lerp at the same time
+ Card.color doesn't need to be a color
+ grid X,Y to screenX,screenY func

x draw Pile accept 
x Deal
x Fan none, right, down, waste
x VariantInfo
x Card flipping animation
x //go:embed Acme.ttf, raccoon280x180.png
x sol/assets directory
x slice of card owners
x Draw the Piles and THEN the cards
x go sort instead of bubble
x detect tap
x keep all positions as int, cast to float64() in Draw()?
x why isn't the top card on top? findCard needs to go backwards
x 1.16 embed png