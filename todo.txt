+ tooltips over hovered icons triggered by (wb *WidgetBase) Update()
  tooltipManager
+ interface for scalable/retro cards
  card face img provider
  card back img provider
+ open url in browser https://gist.github.com/hyg/9c4afcd91fe24316cbf0
+ fan waste after pop
+ don't save virgin games to json? would need to delete any existing saved game
+ Card.UniqueRect() get area of card shown, with y1 == next card's y0
+ WASM
  [ ] json_browser.go
  [x] embed icons
  [ ] http server
+ Settings
  Card design (add default) (a slide out picker)
  Animation speed (a slide out picker for slow, medium, fast)
+ Menu items or toolbar buttons for save/load position (latter will require tooltips)
+ use of Variant is a mess
  TheUserData.Variant and Baize.Variant are the same thing
  SaveableBaize.Variant is saved in json but never checked, and appears in every pile (is Seed needed too?)
+ widget align
  left/center/right (titlebar)
  vertical (navdrawer)
  vertical with left padding, container can resize width to fit (picker)
  vertical with left padding, lines wrap (rules, statistics)
+ cannot tap on an animating card, leaves it out of place
+ Freecell, there was a card in a cell which was not
+ pre-queue for toasts, or add with goroutine, chan and time.Sleep(time.Second) YAGNI
+ Collect() counts as one move, wrong for statistics
+ Scrim rect when nav drawer open? Will sponge up taps and drags. Maybe just alpha down the baize.
+ UI modal navigation drawer (blocks baize from dragging, tap on baize closes)
  https://material.io/components/navigation-drawer#usage
+ you have started N games of X? Loading a varaint should not count as starting
  AfterUserMove() > TheStatistics.startGame() is where it's incremented
+ completed Limited.json is 770KBytes - zip it?
  https://stackoverflow.com/questions/56994992/gzip-json-payload-before-post
+ https://en.wikipedia.org/wiki/Bisley_(solitaire)
+ https://en.wikipedia.org/wiki/Aces_Up
+ Pile iterator eg Baize.IteratePiles("Foundation")
+ cleaner desktop/browser userdata/statistics load/save (interface?)
+ card font scaling could use some more love to remove majic numbers
  includes position of accept symbol
+ Yukon One Suit, Yukon Cells, Russian?
+ tap a scrunched card to show it in full
  another round of Card.Draw, this time draw cards that are currently being tapped (use area minus overlapped area, i.e. rect of text card)
  hover won't work on mobiles
  in Yukon, you can just drag away the card(s) on top of it
+ Spider/Simple Simon tap on King to send conformant pile to foundation (for consistency)
+ by convention Baize.Pile[0] is always the stock
+ Accept might be '1' (Martha) '=' (Spider/Scorpion, could just be 13/K) or 'x', use magic numbers, Accept=0 (or missing) means accept anything
+ map of default Build, Drag for Pile Classes, Build/Drag methods?

+ widget x,y should be in relation to their parent, yes?
  add parent x,y before draw, hit test
+ CardState resting, lerping, flipping in, flipping out, shake left, shake right, shake center
  ... but can flip and lerp at the same time
+ grid X,Y to screenX,screenY func
+ orientation change
+ UI (https://ebitenui.github.io/) titlebar (hamburger, title, undo), statusbar, toast, slideout menu, dialogs, FAB
  either use package or roll-your-own using ebitenui as a guide
+ Pile interface
  PileBase type
  Stock, StockSpider, Waste, Tableau, Foundation, Reserve, Cell types that "has a" PileBase
  Too much duplication between "derived classes"

x show star FAB when loaded game is complete
x widget that displays retro card backs (img, name), card back picker
x RestartGame() now just unwinds undo stack and does not count as a loss
x count of cards in stock toast
x show statistics toast (reshow welcome)
  info button in toolbar instead of help?
x statusbar: stock card count (if not hidden), number of moves, % complete
  NO - takes up too much screen room and looks ugly
x icons.go creates and writes embeddedicons.go
x retire WindowWidth/Height from Game, only need them in main
x Auto scaling of cards to window size (what - even retro ones? Hmmm)
x don't scale the retro cards with fogleman/gg, just create a big LeftMargin
x BUG complete is true when multiple cards in Waste that aren't conformant
x toastManager bump timeout if toast is already displayed
x auto-scaling of cards to window width
  detect changes to window width (either resize window or change orientation)
x add vscroll to baize (not an actual vscroll, just allow it to be dragged up/down)
x retire Variant.DisplayName
x use logoBytes to draw Ace of spades
x draw flipping cards over transitioning cards
x only allow Collect() when tableaux is conformant, FAB
x Use material icons instead of unicode glyphs in UI
  https://material.io/resources/icons/?style=baseline
  use android png (no clear way to draw svg into image)
  store then in a map, pass icon name when creating widget
  icons will need to be scaled to 48x48 by fogleman/gg Scale() (check this works ok first)
  read direct from undo-black-android.zip
x flicker when undoing waste fan (draw flipping cards after transitioning ones)
x Draw-level hit testing of ui widgets
x spider build flags to replace FoundationSpider, tidy Conformant()
x Bookmark / Goto bookmark instead of SavePosition?
x BUG Spider Conformant FAB
  If Baize has FoundationSpider, then Tableau must end with contain 13 conformant cards
x UI FAB widget
  https://material.io/components/buttons-floating-action-button
x modal dialog to show statistics (rejected)
x english rules
x add Text widget for multiline text
x two types of containers; bars and drawers
  navdrawer, picker, settings, statistics, rules are drawers
  BarBase (doesn't do dragging, only Tapping) and DrawerBase (does Dragging and Tapping); NavDraw, Picker, Rules, Settings
  NavDrawer overrides StartDrag() to return false
  ui.drawer points to current open drawer
  can still have Container interface (for dragging and parents)
  retire window, picker, modal
x better passing of things from ui to baize, instead of just a string, passing two strings (for now) as can only pass a value, not a type
x Forty and Eight
x Drag the modal container (by offsetting it's widgets), not the widgets
x ContainerBase
x Widget disabled (drawn in grey) status
x reduce widget boilerplate clutter with method expression/method value/embedded struct
  https://travix.io/type-embedding-in-go-ba40dd4264df
  maybe widgets should draw themselves, instead of painting to their parent
  a widget is just a rectangle, placed at x,y by it's parent, that responds to taps, can be disabled, can be visible or not, 
  so there is a base set of widget functionality that all share: draw, rect,  ...
  containers (like toolbar or nav drawer) should be widgets too?
x don't care if tap happens over toolbar or navdrawer, only if tap happens on their widgets
  so the widget should input.Add() (or Remove() if not visible)
x separate FontBank package? Schriftbank
x stroke, input in separate pkg, so they are visible to UI
x specify card width, then set card height automatically
x FreeCellEasy
x encode prone in <pack><suit><ordinal> three 4-bit numbers
  pack 1..8 0b1111 four bits
  use numbers for suit 1..4 (or 1..8 for star, circle &c) 0b1111 four bits
  ordinal 1..13 0b1111 = 15 four bits
  <pack> << 8 + <suit> << 4 + <ordinal>
  
  id = uint32

  Card.Pack() = id & 0xb111100000000 >> 8
  Card.Suit() = id & 0xb000011110000 >> 4
  Card.Ordinal() = id & 0b1111
  https://play.golang.org/p/5XjdhoDDkq suggests okay to jsonisze uint32
  
x added stats.Lost, retired stats.Played
x util.Pluralize(word string, n int) string {}
x record won game when detecting Complete(), disallow undo?
x append 'u' or 'd' to Card.ID to create SaveableCard (which beomes a string)
x saving a "complete" game, reloading it and it's "started"
  saved state; last entry is "State": 1
  possible fix; Complete (setting state) came after post-move UndoPush()
x save game to json when closing (will need way to specify and switch variant)
x UndoPush() after collect?
x Load/Save Baize
x bury/disinter cards
x Baker's Dozen, variant display name
x Spider Scrunch 5
x detect game exit
x Collect should work with FoundationSpider
x get Build and Drag rules when pile is created
x collect (triggered by command)
x statistics (copied from Opsole), Baize.state 
x BUG buildFlags 1xx used for both rank wrap AND power moves!!
x CompleteWhen attribute, default 0, 13 for Foundations
  other rules for Poker, GrandfathersClock
x should Canfield, Storehouse tableaux have rank wrap?
x investigate nasty panic when using uppercase Card.id
  probably because BuildScalableCardImages() still using lowercase
x Deal string to parse 123456789abcd
x Storehouse (or Thirteen Up), in which one should remove the deuces (twos) and place them on the foundations.
  The reserve and the cards on the tableau are then dealt. The stock is dealt one card at a time, and it can be used only twice.
  Furthermore, the method of building in this game is by suit. The Storehouse variant makes the game easier than Canfield.
x bug fix, NewGame() was setting new seed AFTER shuffling cards
x powerMoves should be in Pile.CanAcceptTail(), because number of moves available is not known until destination pile is known
  that makes power moves flag a build attribute, not a drag attribute
x if a card starts shaking while lerping or being dragged, it doesn't settle onto the pile properly
  ban tapping on an Animating card? didn't work!
  ban starting a drag on an animating card
x scrunchPercentage int (100=no scrunch 10=max scrunch)
x SimpleSimon
x No complete notification for Wasp
x animate move pile cards back to stock when restarting game/starting new game (Pile.Reset())
  needs a separate thread?
x use lerpStep < 1.0 instead of lerping
x 3 char Card.ID, uses base-16 for pack and ordinal
x Card.SetPosition() now terminates an ongoing lerp
x undo on a waste with cards > 4 still isn't right
x undo a spider stock tap: tableau cards are flipped THEN transitioned, why?
x Waste fanning is horrible, especially during undo
x EasyWin variant
x added separate ordinal and symbol fonts
x bug: cannot drag cards to yourself when src pile is above dst pile
x Card.color doesn't need to be a color
x recycle graphic ♲ U+2672
x bug: can drag 1 card from stock to waste when CardsToMove = 3
x Canfield, AutoFillFrom, RankWrap, FoundationCanfield/SetAccept()
x New Undo mechanism based on >1 undo stack, AfteruserMove() and crc32 checksum
x replace acmefonts with scaled cardFonts (regular and large)
x func init() in card.go and scalable.go run before setting CardWidth,Height
x bitmap cards do not scale well, draw cards using fogleman/gg
  96/71 = 1.352112676 for retro
  135/100 default for scalable
x StockSpider check for empty tableaux
x separate settle Card transition? Just go faster if distance < card diagonal
x Australian (diff Build/Drag rules)
x tap card to move to foundation
x len(p.Cards) := p.CardCount()
x Can Flip be handled only by Pop & Push (except during Deal())
  Copy Flip from Opsole, at least it works:
  Just a flip() command, which changes .prone *before* animation
  ignore flip in Foundation.Push, it's just for geezer
  deal flips up/down cards taken from stock
  flip up cards when complete for physics frippery
  UpdateFromSaved corrects flip
  reserve and tableau Pop flips card if it becomes exposed by Pop (by peeking stack after pop)
  stock push flips cards down
  stock pop flips cards up
x Seed, RestartGame(), NewGame(), NewVariant()
x undo
x Pile localAccept, localRecycles
x Flip animation shows wrong face for first half, draw refers to a state, not prone flag
x Shake state 0 (rest), 1 (moving left), 2 (moving right), 3 (moving to center)
x Can drag from stock to tableau; TapTarget := generic Target
x shadow under moving card
x currently can drag Tail off Foundation, inspite of Move=0
x GetIntAttribute needs to return int, bool
x make a Pile.tail []*Card slice when dragging starts?
x Don't expose Stock card after tap
x determine Case of attribute names and values (use PascalCase) (https://en.wikipedia.org/wiki/Naming_convention_(programming))
x retire CardOwner interface; you're abusing interfaces and getting confused by them
  have one Pile type (with a Class member)
  createCards, shuffleCards become classless helper functions
  keep variant attributes as Pile.attributes map 
  PileInfo contains Class, X, Y, Fan, attributes map
x Baize.CardTapped() on Stock.Card not working after recycle
x should CardOwner be *CardOwner? No, interfaces are different.
x simpler card mover
x auto FlipUp of exposed tableau (all?) cards
x dragging >1 card
x draw dragging cards last so they appear on top
x moving card from pile to pile
x pile method probe with type assertion on dumy interface
x CTQ
x remove image.Point from stroke
x draw Pile accept
x Deal
x Fan none, right, down, waste
x VariantInfo
x Card flipping animation
x //go:embed Acme.ttf, raccoon280x180.png
x sol/assets directory
x slice of card owners
x Draw the Piles and THEN the cards
x go sort instead of bubble
x detect tap
x keep all positions as int, cast to float64() in Draw()?
x why isn't the top card on top? findCard needs to go backwards
x 1.16 embed png
